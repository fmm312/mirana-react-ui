import React from 'react';
import { FormLabel } from '..';

export default { title: 'Components/FormLabel', component: FormLabel };

export function Default() {
  return <FormLabel>Label here!</FormLabel>;
}
